planner_prompt = """
    make steps
"""


planner_response = LLM.respont(planner_prompt, userQuery) # should return a py list of steps

planner_response = safe.safeSteps(planner_response)

return planner_response